<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MarkdownStudio.Views.KeyboardShortcutsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"    />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="16,14,16,8" Spacing="2">
            <TextBlock Style="{StaticResource MdsSidebarTitleStyle}" Text="Keyboard shortcuts" />
            <TextBlock Style="{StaticResource MdsCaptionStyle}"
                       Text="Click a shortcut to remap it. Esc to cancel recording." />
        </StackPanel>

        <!-- Conflict prompt — only shown when the user records a chord that's
             already taken by another command. Reassign reassigns and unbinds
             the other command; the InfoBar's close (X) cancels the change. -->
        <muxc:InfoBar x:Name="ConflictBar" Grid.Row="1"
                      Margin="16,4,16,4"
                      IsClosable="True"
                      IsOpen="False"
                      Severity="Warning"
                      Title="Already in use"
                      CloseButtonClick="OnCancelConflictClicked">
            <muxc:InfoBar.ActionButton>
                <Button x:Name="ReassignButton" Content="Reassign"
                        Click="OnReassignClicked"
                        Style="{StaticResource MdsAccentButtonStyle}" />
            </muxc:InfoBar.ActionButton>
        </muxc:InfoBar>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="8,4,8,8">
            <StackPanel x:Name="RowsHost" Spacing="2" />
        </ScrollViewer>

        <Grid Grid.Row="3" Padding="16,10,16,14" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Style="{StaticResource MdsCaptionStyle}"
                       Text="Saved automatically. Settings sync across sessions." />
            <Button Grid.Column="1" x:Name="ResetAllButton"
                    Content="Reset all to defaults"
                    Click="OnResetAllClicked"
                    Style="{StaticResource MdsGhostButtonStyle}"
                    BorderBrush="{ThemeResource MdsBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="10,6" />
        </Grid>
    </Grid>
</UserControl>
