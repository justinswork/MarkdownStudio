# Build output
bin/
obj/
*.user
*.suo
*.cache
.vs/
*.binlog

# Packaging
AppPackages/
BundleArtifacts/
appxout/
release/
*.appx
*.appxbundle
*.msix
*.msixbundle
*.appxupload
*.cer
*.pfx

# Generated by setup-web.ps1 (run `pwsh setup-web.ps1` to recreate)
MarkdownStudio/Web/editor/monaco/
MarkdownStudio/Web/preview/lib/
# (App icons under MarkdownStudio/Assets/ are real artwork and tracked in git.)

# Debug / scratch
layout.log
screenshot-*.png

# Design source files (Photopea/Photoshop PSDs, Figma exports, etc.) —
# kept locally next to the code for easy iteration but not committed
# because they're heavy binaries that don't diff well.
design/
